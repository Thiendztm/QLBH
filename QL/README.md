# Sales Management (MySQL)

This folder contains the database-focused project skeleton. The priority is SQL, not web/UI.

## Suggested workflow
1. Define schema in schema/ (tables, constraints, indexes).
2. Load sample data in data/.
3. Write queries in queries/.
4. Use scripts/ for helper SQL (create/drop DB, seed order).
5. Keep documentation in docs/.
